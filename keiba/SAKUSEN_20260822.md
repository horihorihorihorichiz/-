# 8/22(土) 作戦書 — 新潟・中京・札幌（起きたらこれだけ読めばいい）

夜のうちに仕込んだもの・朝やること・買う条件を1枚に。**購入・GOは人間**（規約どおり）。

---

## 0. 夜に仕込み済みのもの

| 仕込み | 状態 |
|---|---|
| オッズ時系列の収集（36R・684時点・初回採取6:40） | 番人(keep_alive)監視下で稼働。**今日が初収集日** |
| 明日36レースの出馬表+過去9走の先取り | 夜間に自動取得（race_2026*.json）。朝の処理が速くなる |
| 2022/2023収穫の埋め戻し | 番人監視下で継続。溜まり次第、凍結セルの独立検証に使う |
| 夜間監視モニタ | 番人が死んだら自動でセッションが起きて再起動する |

## 1. 朝のチェックリスト（ルーチンが動かなかった場合の手動手順）

```bash
cd keiba
tail -n 3 logs/keep_odds822.log        # ①「NNレース / 予定NNN時点」が出ているか（最優先）
python3 selfcheck.py                   # ② ALL GREEN
python3 day_board.py                   # ③ 当日ボード
```

## 2. 今日の「当てにいく」条件（3期間で再現済みの的中率セル）

**軸1位流し26**（モデル1位軸・紐2〜6位の三連複10点・1000円）
- 条件: **8頭以下 かつ モデル1位の単勝2倍未満**
- 実測的中率: 74% / 83% / 62%（学習期/未知2期。全レース平均25%の約3倍）
- ROIは70〜110%なので**紙上（paper_rank）が既定**。実弾は人間の判断。

**BOX5三連複**（モデル上位5頭ボックス10点）
- 条件: **8頭以下 かつ 1位単勝2〜3倍**（的中67/54/75%）

→ 今日の該当レースは朝の day_board と各レースのオッズで確定する。
   「8頭以下」は今夜の先取りで既に分かる（出馬表参照）。オッズ条件はT-30で判定。

## 3. 凍結12セルのうち、今日ライブで発火しうるもの（掛け金ゼロで記帳）

今日の開催は新潟・中京・札幌。凍結セル（mine200_watch.json）のうち該当可能性:

| セル | 券種 | 発火条件（当日確認） |
|---|---|---|
| 1300-1400m×1位3-5倍×**中京** | 三連複123(1点) | 中京の1300-1400m戦で1位オッズ3-5倍 |
| 15-16頭×芝M×**中京** | 軸1位流し26 | 中京の芝1500-1700mで15-16頭 |
| 稍重×1位3-5倍×**新潟** | 三連複123(1点) | 新潟が稍重になった場合のみ |

**注意: この12セルは乱数と区別がついていない**（null期待20 vs 実データ12）。
だから実弾ゼロ・記帳のみ。当たっても外れても「新データでの実測」が貯まることが成果。

## 4. 全レース共通の禁止事項（実測済み・これだけで数%変わる）

1. **単勝120倍超の馬が絡む買い目は買わない**（複勝ROI47%・単勝38.7%の捨て場）
2. **三連複BOXで手広く買わない**（高倍率の組が混ざるほどROIが下がる。実測: 合成2000倍超の組はROI 22-45%）
3. **開催初日は要注意**（既定ルール）。今日は新潟3回1日目・中京3回1日目・札幌1回2日目 → **新潟・中京は初日**。馬場読みが効かないので凍結ルールブックの判定に従う。

## 5. 今日いちばん大事なこと（勝ち筋の話）

今日の的中・不的中より大事なのが**オッズ時系列の初日データ**。
全2,200R×全券種×全値段帯×地方まで測って、確定オッズだけでは控除率を超えられないことが
確定している（WALL_20260821.md）。残っている唯一の未検証が「**発走前の金の流れ**」で、
その収集が今日から始まる。6:40の初回採取から発走直前まで、1レース19時点。
夕方に `python3 odds_timeline.py settle && python3 odds_timeline.py stats` で
「判定時刻のオッズが確定オッズをどれだけ偏って推定するか」の初回実測が出る。
判定に必要な3,000R（8〜10開催日）へ向けた1日目。

## 6. 夕方（全レース終了後）

```bash
python3 odds_timeline.py settle && python3 odds_timeline.py stats   # ★今日の主目的
python3 paper_rank.py settle && python3 paper_rank.py stats
python3 v99w_rank.py settle && python3 v99w_rank.py stats           # 6レーン(V100含む)
python3 w12_watch.py settle && python3 w12_watch.py stats
python3 mine200_check_new.py    # 収穫が進んでいれば凍結セルの独立検証も更新
```
