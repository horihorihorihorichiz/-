# 採掘キャンペーン事前登録書（2026-08-17 13:30 JST・データ拡張前にコミット）

ユーザー指示: 「過去レースも未来と同じ事前情報で分析」「色んな条件の色んな得点配分」「未来の回収率200%超を目指す」。
本書は**拡張データを見る前に**探索範囲と合格基準を固定する事前登録。後知恵禁止ルール(RULES.md)の適用第1号。

## データ
- wf_preds_v3ext2.jsonl（構築中）: 月次ウォークフォワード 202409〜202608 見込み。
  各レース=その時点までのデータだけで学習したV3モデルの事前スコア＋確定オッズ＋払戻。
- 検証の3分割（時系列固定・変更禁止）:
  - **MINE**: 202409〜202602（採掘に使ってよい）
  - **VALIDATE**: 202603〜202605（採掘後に1回だけ照合）
  - **CONFIRM**: 202606〜202608（VALIDATE通過セルのみ1回だけ照合）
- 最終合格はさらにその先: **ライブ紙上運用（直前判定・凍結後）でのROI**。内部3分割を全部通っても
  「候補」であり、実測はライブのみ。

## 探索グリッド（これ以外のセルを後から足さない）
- 条件軸: 馬場(芝/ダ) × 距離帯(短/マイル/中/長) × 頭数帯(〜8/9-12/13-16/17-18) × クラス(tier) ×
  開催日(初日/2日目+) × 場グループ(中央4場/ローカル) × baba(良/道悪)
- シグナル軸: g12帯 × sp15帯 × p1帯 × モデル1位オッズ帯(3-5/5-8/8-12/12-20/20+) ×
  モデル2位オッズ帯 × 市場1位オッズ(1.5未満/1.5-3/3+)
- 買い方: 単勝1位/単勝2位/複勝1位/馬連1-2位/三連複1位軸
- 配点: セルごとに flat 100円で採掘。合格セルのみ、期待エッジ比例(ケリー1/4上限)の配分を最後に設計。

## 合格基準（事前固定）
1. MINE: n≥40・ROI≥130%・オッズシャッフルnull検定p<0.05
2. VALIDATE: n≥15・ROI≥110%（1回だけ計算。落ちたセルの再調整禁止）
3. CONFIRM: n≥10・ROI≥100%（同上）
4. 多重比較への対処: 試行セル総数を必ず記録・報告。VALIDATE通過数が「偶然の期待通過数
   (試行数×p)」の3倍未満なら、キャンペーン全体を不合格として公表する。
5. 通過セルの主張ROIは**VALIDATE+CONFIRMの合算値**（MINEの数字は使用禁止）で表示し、[候補]ラベル。

## 200%目標について（正直な注記）
- 台帳全体でモデル1位は市場に全敗(ANALYSIS_20260816.md)。200%超は「広い買い方」では
  ほぼ不可能で、狙いは「まれに発火する高エッジセルの束」。発火頻度が低いほど分散は大きく、
  少数レースの200%は運と区別しにくい。だからこそ上記3段+ライブの4関門を全部通す。
- 関門を通るセルがゼロならゼロと報告する。数字は作らない。

## 実行
- 第一段(実行中): 週末8週分の収穫 → enrich/jockey/speedidx → 台帳202409拡張ダンプ
- 第二段: 本書のグリッドを機械探索（miner_v2.py・これから作成）→ 3分割照合 → 報告書
- 第三段: 通過セルがあれば配点設計 → RULEBOOK_V2候補として凍結手続き(ハッシュ+窓端202608記録) →
  ライブ紙上で直前判定運用開始
